package org.monitoring.catchholebackend.domain.character.type;

/** 설정 후보의 표시값과 구조화 값이 현재 schema 계약을 만족하는지 나타낸다. */
public enum SettingCandidateValueValidationStatus {
    VALID,
    INVALID,
    NOT_APPLICABLE
}
